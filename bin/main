#!/usr/bin/env ruby

def fizz_buzz(num)
  num = (1..num)
  num.each do |i|
    if (i % 3).zero? && (i % 5).zero?
      i = 'FizzBuzz'
    elsif (i % 3).zero?
      i = 'Fizz'
    elsif (i % 5).zero?
      i = 'Buzz'
    end
    puts i
  end
end

fizz_buzz(100)
